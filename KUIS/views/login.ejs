<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #fef9d9;
        }

        h2 {
            text-align: center;
        }

        form {
            display: flex;
            flex-direction: column;
            width: 300px;
            margin: 0 auto;
        }

        input {
            margin: 5px 0;
            padding: 10px;
            font-size: 16px;
        }

        button {
            padding: 10px;
            background-color: #2884e7;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color:  #064a6e;
        }

        label {
            font-weight: bold;
            margin-top: 10px;
        }

        .container {
            background-color: #8eaccd;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgb(6, 94, 152);
        }

        p {
            text-align: center;
        }

        a {
            color: #c0e6f9;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Login</h2>
        <form action="/auth/login" method="POST">
            <label for="username">Username</label>
            <input type="text" id="username" name="username" placeholder="Masukkan nama admin" required>

            <label for="email">Email</label>
            <input type="email" id="email" name="email" placeholder="Masukkan email admin" required>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" placeholder="Masukkan kata sandi" required>

            <button type="submit">Login</button>
        </form>
        <p>You Don't Have an Account? <a href="/auth/register">Register Here</a></p>
    </div>
</body>
</html>
